<?php

return [

    /*
    |--------------------------------------------------------------------------
    | 签名校验是否开启
    |--------------------------------------------------------------------------
    |
    | 本配置将决定接口的签名校验是否开启
    |
    */
    'app_sign' => env('APP_SIGN', false),

    /*
    |--------------------------------------------------------------------------
    | 签名客户key
    |--------------------------------------------------------------------------
    |
    | 本配置在生成签名的时候将使用
    |
    */
    'client_key' => env('SIGN_CLIENT_KEY', 'signclientkey'),

    /*
    |--------------------------------------------------------------------------
    | 需要忽略的签名路由
    |--------------------------------------------------------------------------
    |
    | 本配置将决定已本前缀开头的了路由不参与校验签名
    |
    */
    'forget_prefix' => env('SIGN_FORGET_PREFIX', 'unsign/*'),

];
